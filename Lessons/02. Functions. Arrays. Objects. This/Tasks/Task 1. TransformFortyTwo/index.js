const transformFortyTwo = (numbers) => {
    // Начало
  for(let i=0; i<numbers.length; i++){
   
    if(numbers[i]%42==0){
      numbers.toString();
        numbers[i]=`forty two!`;
    }
 }
   return numbers;
    
    // Конец
};


export default transformFortyTwo;